services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - appdata
      - ai-service
    networks:
      - ai-network
    environment:
      - REACT_APP_API_URL=http://appdata:6001/api
      - REACT_APP_AI_SERVICE_URL=http://ai-service:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules

  appdata:
    build: 
      context: ./appData
      dockerfile: Dockerfile
    ports:
      - "6001:6001"
    volumes:
      - ./appData:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=6001
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/aiautoapply
    env_file:
      - ./appData/.env
    networks:
      - ai-network
    depends_on:
      - postgres
      - redis

  ai-service:
    build: 
      context: ./ai-service
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./ai-service:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=8000
      - APPDATA_API_URL=http://appdata:6001/api
    env_file:
      - ./ai-service/.env
    networks:
      - ai-network
    depends_on:
      - appdata

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - ai-network

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aiautoapply
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ai-network

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - ai-network

networks:
  ai-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
